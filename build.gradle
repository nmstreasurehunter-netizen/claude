// Racine du projet TraceCroquis - compatible AndroidIDE / JDK 17
plugins {
    id 'com.android.application' version '8.0.2' apply false
}

tasks.register('clean', Delete) {
    delete rootProject.buildDir
}
